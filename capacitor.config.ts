import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.vue',
  appName: 'tabsTestVue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
